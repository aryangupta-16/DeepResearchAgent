apiVersion: 1

# Grafana provisioning (Phase A): Prometheus datasource, auto-wired.
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
